{
  "name": "equalify-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:prod": "esbuild index.ts --bundle --platform=node --outdir=dist --external:@aws-sdk --loader:.node=file && cd dist && zip -r lambda.zip index.js > /dev/null && aws --profile equalifyuic lambda update-function-code --function-name equalifyuic-api --zip-file \"fileb://lambda.zip\" > /dev/null && rm -rf lambda.zip",
    "build:staging": "esbuild index.ts --bundle --platform=node --outdir=dist --external:@aws-sdk --loader:.node=file && cd dist && zip -r lambda.zip index.js > /dev/null && aws --profile equalifyuic lambda update-function-code --function-name equalifyuic-api-staging --zip-file \"fileb://lambda.zip\" > /dev/null && rm -rf lambda.zip",
    "build": "yarn build:staging && yarn build:prod"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "type": "module",
  "devDependencies": {
    "@types/node": "^24.5.2",
    "aws-jwt-verify": "^5.1.0",
    "cheerio": "^1.1.2",
    "esbuild": "^0.25.10",
    "openai": "^5.22.0",
    "serverless-postgres": "^2.1.2",
    "string-strip-html": "^13.4.13",
    "uuid": "^13.0.0"
  },
  "imports": {
    "#src/*": "./*"
  }
}
