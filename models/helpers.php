<?php

/**************!!EQUALIFY IS FOR EVERYONE!!***************
 * This document includes helpers that allow integrations
 * to access Equalify.
 * 
 * As always, we must remember that every function should 
 * be designed to be as effcient as possible so that 
 * Equalify works for everyone.
**********************************************************/

/**
 * Add Equalify Alert
 */
function add_equalify_alert(
    $source, $url, $integration_uri, $type, $message, $meta
){

    // Execute the process to add an alert.
    exec();

}